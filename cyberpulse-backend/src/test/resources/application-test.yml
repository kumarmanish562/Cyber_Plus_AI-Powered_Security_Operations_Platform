spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cyberpulse
    username: postgres
    password: 0328
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate